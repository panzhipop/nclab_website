---
const { title, image } = Astro.props;
---

<section
  class="hero"
  style={image ? `background-image:
    linear-gradient(105deg, rgba(15,60,115,.94) 0%, rgba(15,60,115,.74) 100%),
    url('${image}'); background-size: cover; background-position: center;` : undefined}
>
  <div>
    <h1>{title}</h1>
  </div>
</section>
